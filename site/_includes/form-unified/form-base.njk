{# Import macros. #}
{% import "form-unified/field-checkbox.njk" as checkbox %}
{% import "form-unified/field-radio.njk" as radio %}
{% import "macros/form-action.njk" as action %}
{% import "macros/form-submit.njk" as submit %}

{# Begin HTML. #}
<engca-form-unified>

  {# HEADING. #}
  <h2>{{ 'join-the-conversation' | i18n }}</h2>
  <p>{{ 'join-the-conversation-form-label' | i18n }}</p>

  {# FORM. #}
  <form
      method="post"
      id="mc-embedded-subscribe-form"
      name="mc-embedded-subscribe-form"
      target="_self"
      {# Change data-env value to "test" to use test variables in index.js. #}
      data-env="test"
      novalidate
  >

    {# Email. #}
    {% include 'form-unified/email.njk' %}
    {# - #}

    {# Which discussion #}
    <div data-form-section="discussion">
    <h3 class="form-title">{{ 'topics-interests-label' | i18n }}</h3>
    <ul class="form-checkbox-list">
      <li>{{ checkbox.data('audience', 'fires', formVars.label_fires) }}</li>
      <li>{{ checkbox.data('audience', 'employee', formVars.label_employee ) }}</li>
      <li>{{ checkbox.data('audience', 'future', formVars.label_future ) }}</li>
    </ul>
    </div>
    {# - #}

    {# Fires options. #}
    <div data-form-section="fires">
    <legend class="form-control-label">
      <span aria-hidden="true">*</span>
      <span class="sr-only">{{ 'required-label' | i18n }}:</span>
      {{ 'topic-los-angeles-fires-recovery-evacuation-label' | i18n }}
    </legend>
    <ul class="form-radio-list">
      {{ radio.merge_field_data('interest-fire', 'eaton', 'topic-los-angeles-fires-recovery-evacuation-eaton' | i18n, "required" ) }}
      {{ radio.merge_field_data('interest-fire', 'palisades','topic-los-angeles-fires-recovery-evacuation-palisades' | i18n, "required") }}
      {{ radio.merge_field_data('interest-fire', 'no','topic-los-angeles-fires-recovery-no-evacuation' | i18n , "required" ) }}
    </ul>
    </div>
    {# - #}

    {# State employee option. #}
    <div data-form-section="employee">
    <legend class="form-control-label">
      <span aria-hidden="true">*</span>
      {{ formVars.label_employee_question }}
    </legend>
      <ul class="form-checkbox-list">
        <li>{{ checkbox.data('interest-employeeYes', 'employeeYes', formVars.label_yes) }}</li>
        <li>{{ checkbox.data('interest-employeeNo', 'employeeNo', formVars.label_no) }}</li>
      </ul>
    </div>
    {# - #}

    {# Error messages. #}
    <engca-form-error id="apiError" hidden>
      <strong>{{ 'form-problem-message' | i18n }}</strong>
      {{ 'form-problem-message-2' | i18n }}
    </engca-form-error>
    <engca-form-error id="errorEmployee" hidden>
      <strong>{{ 'form-problem-state-employee-not-checked' | i18n }}</strong>
    </engca-form-error>
    {# - #}

    {# Submit. #}
    {{ submit.data('form-submit-button' | i18n) }}
    {# - #}

  </form>
  {# = #}

  {# SUCCESS. #}
  <engca-form-success aria-live="polite"></engca-form-success>
  <template id="form-success-msg">
    <p>
      <strong>{{ 'form-success-message' | i18n }}</strong>
    </p>
    <img width="75" height="75" src="/public/images/check-circle.svg" aria-hidden="true" alt=""/>
    <p>{{ 'form-success-message-2' | i18n }}</p>
  </template>
  {# = #}

</engca-form-unified>
{# - #}

{# Privacy. #}
<p>
  {{ 'form-privacy-message' | i18n | safe }}
  {{ 'form-privacy-message-1' | i18n | safe }}
</p>
{# - #}
