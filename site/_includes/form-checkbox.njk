{% macro data(i8nkey, id, name, checked) %}
    <input type="checkbox" class="form-check-input" id="mce-group[91023]-91023-{{ id }}" name="group[91023][{{ name }}]" value="{{ i8nkey }}" {{ checked }}>
    <label class="form-control-label form-checkbox-label" for="mce-group[91023]-91023-{{ id }}">
        {{ i8nkey }}
    </label>
{% endmacro %}